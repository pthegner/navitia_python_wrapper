#!/usr/bin/env python

from setuptools import setup, find_packages

setup(
    name='navitia_wrapper',
    packages=find_packages(),
    author_email='dt.ro@canaltp.fr',
    description='Python wrapper around the navitia API (api.navitia.io)',
)

